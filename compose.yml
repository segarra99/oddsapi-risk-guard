services:
    app:
        image: odds-feed-risk-guard
        container_name: odds-feed-risk-guard
        build: .
        ports:
            - "3000:3000"
        env_file:
            - .env
        volumes:
            - ./src:/app/src
        develop:
            watch:
                - path: ./src
                  action: rebuild
